{% extends 'base.html.twig' %}

{% block title %}Hello MovieController!{% endblock %}

{% block body %}
<div class="container">
    <img src="images/{{ movie.poster }}" class="card-img-top" alt="...">
    <h5 class="card-title">{{ movie.name }}</h5>
    <p class="card-text">{{ movie.synopsis }}</p>
</div>
{% endblock %}
